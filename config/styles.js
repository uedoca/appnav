import { StyleSheet } from "react-native";

// crio a constante de estilos 
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});

// exporto posteriormente
export default styles;
